# Case study: k-NN graph from LLM embeddings.
# Placeholder — training raises NotImplementedError until wired up.
name: knn_llm

embedding_path: null
k: 10
lr: 0.01
epochs: 200
weight_decay: 5e-4
use_early_stopping: true
patience: 50
