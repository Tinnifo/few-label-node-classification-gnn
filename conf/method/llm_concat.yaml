# Case study: concatenate LLM embeddings with node features.
# Placeholder — training raises NotImplementedError until wired up.
name: llm_concat

embedding_path: null          # path to precomputed LLM embeddings (.pt / .npy)
lr: 0.01
epochs: 200
weight_decay: 5e-4
use_early_stopping: true
patience: 50
