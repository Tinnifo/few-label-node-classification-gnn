# A4 leak control: A3 WITHOUT stripping — measures how much of the
# explanation-view "gain" is label leakage (run name gets a -LEAK suffix)
enabled: true
descriptor: tape
encoder: sentence-transformers/all-MiniLM-L6-v2
embeddings: null
keep_label_leak: true
dim: 128
hidden: 256
descriptor_model: ibm-granite/granite-4.2-3b
descriptor_max_tokens: 256
class_names: null
