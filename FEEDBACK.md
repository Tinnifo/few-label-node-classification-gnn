I think direction 2 makes sense. We can motivate the work by saying that the LLM semantic view provides information that may not be captured by the graph structure. The main challenge is how to preserve the useful differences in the semantic view while still combining it effectively with the structural views for classification. We can keep the original contrastive loss between the two structural views in CG3, and add a disparity or HSIC-based loss to stop the LLM view from becoming too similar to them. This way, the two structural views stay aligned, while the semantic view keeps its complementary information.
 
For the experiments, I think the main comparison should focus on whether our method can consistently outperform CG3 and other few-label node classification baselines. We can test this on both homophilic and heterophilic graphs (if we can find suitable text-attributed heterophilic datasets). We can then include a separate section for the different ways of using LLMs mentioned in the document. For example, we can compare our method with directly concatenating LLM embeddings, building a k-NN graph from the embeddings, and using standard feature fusion. I think these are more suitable as case studies or method variants rather than the main baselines.
 
For LLM-based labeling methods, I am not sure they need to be included as a main comparison. They increase the effective number of labels and therefore address a different setting from ours, where the original label budget remains unchanged. It is also difficult to make a fair comparison because the results depend heavily on the selected LLM, prompts, confidence filtering, and possible benchmark contamination. Different models may have seen the datasets, related papers, arXiv versions, or GitHub repositories during training, so it would be difficult to interpret the comparison clearly.
 
You two can discuss the specific method design first and then start the experiments based on direction 2. If there are any concrete ideas or design choices you would like to discuss before next Wednesday, please let me know.
 

